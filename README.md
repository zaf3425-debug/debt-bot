# 💰 Telegram Debt Bot - بوت إدارة الديون

بوت تليجرام لإدارة الديون والدفوعات بطريقة سهلة وبسيطة.

## ✨ المميزات

- ✅ إضافة أشخاص جدد مع مبالغ ديون أولية
- ✅ إضافة ديون جديدة
- ✅ تسجيل الدفوعات
- ✅ عرض حالة الديون (الإجمالي والمدفوع والمتبقي)
- ✅ حذف الأشخاص
- ✅ دعم اللغة العربية كاملاً
- ✅ تشغيل 24/7 مجاناً على GitHub Actions

## 🚀 البدء السريع

### المتطلبات
- Python 3.10+
- رمز بوت التليجرام (من @BotFather)

### التثبيت المحلي

1. استنسخ المستودع:
```bash
git clone https://github.com/zaf3425-debug/debt-bot.git
cd debt-bot
```

2. ثبت المكتبات:
```bash
pip install -r requirements.txt
```

3. أنشئ ملف `bot_token.txt` وأضف فيه رمز البوت:
```bash
echo "YOUR_BOT_TOKEN_HERE" > bot_token.txt
```

4. شغّل البوت:
```bash
python bot_ar.py
```

## 🔑 إعداد GitHub Actions (للتشغيل 24/7)

### الخطوات:

1. **أضف رمز البوت إلى Secrets**:
   - اذهب إلى: `Settings` → `Secrets and variables` → `Actions`
   - اضغط `New repository secret`
   - أضف سر جديد:
     - **Name**: `TELEGRAM_BOT_TOKEN`
     - **Value**: رمز البوت الخاص بك (من @BotFather)

2. **تفعيل GitHub Actions**:
   - اذهب إلى: `Actions`
   - اضغط `I understand my workflows, go ahead and enable them`

3. **التحقق**:
   - البوت سيعمل كل 5 دقائق تلقائياً
   - يمكنك تشغيله يدويًا من `Actions` → `Telegram Debt Bot 24/7` → `Run workflow`

## 📱 طريقة الاستخدام

1. ابدأ البوت: `/start`
2. اختر الأمر المطلوب من القائمة الظاهرة
3. اتبع التعليمات المكتوبة

### الأوامر الرئيسية:
- **قائمة الأسماء**: عرض جميع الأشخاص
- **إضافة اسم جديد**: إضافة شخص بدين جديد
- **الكل**: عرض جميع الديون والمتبقي
- **دين جديد**: إضافة دين إضافي لشخص
- **سداد**: تسجيل دفعة
- **الحالة**: عرض تفاصيل شخص
- **حذف الشخص**: حذف شخص من القائمة

## 💾 البيانات

البيانات تُحفظ في ملف `debts.db` (قاعدة بيانات SQLite محلية)

**ملاحظة**: عند استخدام GitHub Actions، البيانات تُحفظ محلياً أثناء كل تشغيل فقط.

للحفاظ على البيانات بشكل دائم، يمكنك:
- نقل البيانات يدويًا
- استخدام database عن بُعد

## 🐛 استكشاف الأخطاء

### البوت لا يستجيب:
- تحقق من صحة الرمز في `bot_token.txt`
- تحقق من اتصالك بالإنترنت

### المشاكل في GitHub Actions:
- اذهب إلى `Actions` وتحقق من السجلات (Logs)
- تأكد من إضافة `TELEGRAM_BOT_TOKEN` إلى Secrets

## 📝 الملفات الرئيسية

- `bot_ar.py` - كود البوت الرئيسي
- `requirements.txt` - المكتبات المطلوبة
- `.github/workflows/bot-24-7.yml` - ملف الأتمتة

## 📄 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الحر.

## 💬 الدعم

لأي مساعدة أو استفسار، تفضل بفتح issue على GitHub.

---

**ملاحظة مهمة**: 
⚠️ عند استخدام GitHub Actions المجاني، البيانات قد لا تُحفظ بشكل دائم بين التشغيلات المختلفة. 
للحصول على بيانات دائمة، استخدم خيارات الاستضافة الأخرى مثل Oracle Cloud أو Railway.